{
  "domain": "com.tezov",
  "dependencies" : "${file://plugin_project_demo/tezov.catalog.dependencies.yaml}",
  "app": [
    "${plugin.application}",
    "${plugin.kotlin}",
    "${plugin.tezov_project_config}"
  ],
  "lib": [
    "${plugin.library}",
    "${plugin.kotlin}",
    "${plugin.tezov_project_config}"
  ]
}